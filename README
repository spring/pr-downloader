#pr-downloader - tool to download maps and mods for the Spring engine

##compile
to compile, you need libtorrent-rasterbar and boost
	cmake .
	make
	make install

##goals
what it wants to do:

./run-test
fetch + compile latest git
fetch latest test-mod
runs spring + testmod


run-test.exe
fetch + compile latest git
fetch latest test-mod
run spring + testmod


what is needed:

environement to compile spring (bad!)
downloader for mod with tests included

